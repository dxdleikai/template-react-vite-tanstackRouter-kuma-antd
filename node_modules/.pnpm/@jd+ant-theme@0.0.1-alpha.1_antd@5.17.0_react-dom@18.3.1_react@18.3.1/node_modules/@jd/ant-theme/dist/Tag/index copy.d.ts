import { TagProps as AntTagProps } from 'antd';
import React from 'react';
export interface TagProps extends Omit<AntTagProps, 'color'> {
    /**
     * 尺寸
     * @default "small"
     */
    size?: 'small' | 'middle' | 'large';
    /**
     * 在ant 基础上添加primary、secondary颜色
     */
    color?: AntTagProps['color'] | 'primary' | 'secondary';
}
declare const Tag: React.ForwardRefExoticComponent<TagProps & React.RefAttributes<HTMLElement>> & Pick<import("antd").TagType, "CheckableTag">;
export default Tag;
