import { NativeProps } from "../../utils/PropsType";
import React, { FC } from 'react';
export type ThemeEaProps = {
    children?: React.ReactNode;
} & NativeProps;
/**
 * 主题配置
 */
declare const ThemeEa: FC<ThemeEaProps>;
export default ThemeEa;
