function _extends() { _extends = Object.assign ? Object.assign.bind() : function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }
import { getProps } from "../utils/PropsType";
import { ConfigProvider } from "./..";
import { InputNumber as AntInputNumber } from 'antd';
import classNames from 'classnames';
import omit from 'lodash/omit';
import React, { forwardRef } from 'react';
/**
 * 继承ant InputNumber，扩展了size 属性
 * TODO 前后样式需要重写
 */
var InternalInputNumber = function InternalInputNumber(props, ref) {
  var _React$useContext = React.useContext(ConfigProvider.ConfigContext),
    getPrefixCls = _React$useContext.getPrefixCls;
  var prefixCls = getPrefixCls();
  var size = props.size;
  var wrapperProps = getProps(props, {
    className: classNames([size ? "".concat(prefixCls, "-input-number-input-").concat(size) : '']),
    styleStart: {
      // display: 'flex',
      // alignItems: 'center',
      // justifyContent: 'center',
    }
  }, []);
  return /*#__PURE__*/React.createElement(AntInputNumber, _extends({}, wrapperProps, {
    ref: ref
  }), props.children);
};
// 重写组件
var InputNumber = /*#__PURE__*/forwardRef(InternalInputNumber);

// 合并内部方法
Object.assign(InputNumber, omit(AntInputNumber, ['$$typeof', 'render']));
export default InputNumber;