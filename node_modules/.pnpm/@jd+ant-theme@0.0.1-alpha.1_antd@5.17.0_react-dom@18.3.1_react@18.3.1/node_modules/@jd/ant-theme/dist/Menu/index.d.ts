import { Menu as AntMenu, MenuProps as AntMenuProps, MenuRef } from 'antd';
import React from 'react';
export interface MenuProps extends Omit<AntMenuProps, ''> {
    [name: string]: any;
}
type M = Omit<typeof AntMenu, ''> & React.ForwardRefExoticComponent<MenuProps & React.RefAttributes<MenuRef>>;
declare const Menu: M;
export default Menu;
