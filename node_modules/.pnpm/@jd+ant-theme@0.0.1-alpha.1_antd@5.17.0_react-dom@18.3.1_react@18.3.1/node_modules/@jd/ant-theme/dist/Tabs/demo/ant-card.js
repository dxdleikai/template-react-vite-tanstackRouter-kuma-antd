import { Tabs } from "../..";
import React from 'react';
var onChange = function onChange(key) {
  console.log(key);
};
var App = function App() {
  return /*#__PURE__*/React.createElement(Tabs, {
    onChange: onChange,
    type: "card",
    items: new Array(3).fill(null).map(function (_, i) {
      var id = String(i + 1);
      return {
        label: "Tab ".concat(id),
        key: id,
        children: "Content of Tab Pane ".concat(id)
      };
    })
  });
};
export default App;