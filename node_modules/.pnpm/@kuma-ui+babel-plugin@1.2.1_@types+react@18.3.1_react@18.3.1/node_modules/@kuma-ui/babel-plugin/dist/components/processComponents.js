"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/components/processComponents.ts
var processComponents_exports = {};
__export(processComponents_exports, {
  processComponents: () => processComponents
});
module.exports = __toCommonJS(processComponents_exports);
var import_core4 = require("@babel/core");
var import_componentList = require("@kuma-ui/core/components/componentList");

// src/components/handlers/Box.ts
var handleBox = (path) => {
};

// src/components/handlers/Flex.ts
var import_core = require("@babel/core");
var handleFlex = (path) => {
  const openingElement = path.node.openingElement;
  if (!import_core.types.isJSXIdentifier(openingElement.name))
    return;
  if (!openingElement.attributes.some(
    (attr) => attr.type === "JSXAttribute" && attr.name.name === "display"
  )) {
    openingElement.attributes.push(
      import_core.types.jsxAttribute(import_core.types.jsxIdentifier("display"), import_core.types.stringLiteral("flex"))
    );
  }
};

// src/components/handlers/Spacer.ts
var import_core2 = require("@babel/core");
var handleSpacer = (path) => {
  const openingElement = path.node.openingElement;
  if (!import_core2.types.isJSXIdentifier(openingElement.name, { name: "Spacer" }))
    return;
  let size;
  let horizontal;
  openingElement.attributes = openingElement.attributes.filter((attr) => {
    if (import_core2.types.isJSXAttribute(attr)) {
      if (attr.name.name === "size") {
        size = attr;
        return false;
      } else if (attr.name.name === "horizontal") {
        horizontal = attr;
        return false;
      }
    }
    return true;
  });
  const displayValue = horizontal ? "inline-block" : "block";
  const display = openingElement.attributes.some(
    (attr) => import_core2.types.isJSXAttribute(attr) && attr.name.name === "display"
  ) ? void 0 : import_core2.types.jsxAttribute(import_core2.types.jsxIdentifier("display"), import_core2.types.stringLiteral(displayValue));
  const flexShrink = openingElement.attributes.some(
    (attr) => import_core2.types.isJSXAttribute(attr) && attr.name.name === "flexShrink"
  ) ? void 0 : import_core2.types.jsxAttribute(
    import_core2.types.jsxIdentifier("flexShrink"),
    import_core2.types.jSXExpressionContainer(import_core2.types.numericLiteral(0))
  );
  let widthValue, heightValue;
  if (size && import_core2.types.isJSXExpressionContainer(size.value)) {
    widthValue = horizontal ? size.value : import_core2.types.jSXExpressionContainer(import_core2.types.stringLiteral("auto"));
    heightValue = horizontal ? import_core2.types.jSXExpressionContainer(import_core2.types.stringLiteral("auto")) : size.value;
  } else {
    widthValue = horizontal ? import_core2.types.jSXExpressionContainer(import_core2.types.numericLiteral(0)) : import_core2.types.jSXExpressionContainer(import_core2.types.stringLiteral("auto"));
    heightValue = horizontal ? import_core2.types.jSXExpressionContainer(import_core2.types.stringLiteral("auto")) : import_core2.types.jSXExpressionContainer(import_core2.types.numericLiteral(0));
  }
  const width = openingElement.attributes.some(
    (attr) => import_core2.types.isJSXAttribute(attr) && attr.name.name === "width"
  ) ? void 0 : import_core2.types.jsxAttribute(import_core2.types.jsxIdentifier("width"), widthValue);
  const height = openingElement.attributes.some(
    (attr) => import_core2.types.isJSXAttribute(attr) && attr.name.name === "height"
  ) ? void 0 : import_core2.types.jsxAttribute(import_core2.types.jsxIdentifier("height"), heightValue);
  [display, flexShrink, width, height].forEach((attr) => {
    if (attr)
      openingElement.attributes.push(attr);
  });
};

// src/components/handlers/Text.ts
var handleText = (path) => {
};

// src/components/handlers/Button.ts
var import_core3 = require("@babel/core");
var handleButton = (path) => {
  const openingElement = path.node.openingElement;
  if (!import_core3.types.isJSXIdentifier(openingElement.name))
    return;
  if (!openingElement.attributes.some(
    (attr) => attr.type === "JSXAttribute" && attr.name.name === "cursor"
  )) {
    openingElement.attributes.push(
      import_core3.types.jsxAttribute(import_core3.types.jsxIdentifier("cursor"), import_core3.types.stringLiteral("pointer"))
    );
  }
};

// src/components/handlers/Heading.ts
var handleHeading = (path) => {
};

// src/components/processComponents.ts
var processComponents = (nodePath, importedStyleFunctions) => {
  nodePath.traverse({
    JSXElement(path) {
      const openingElement = path.node.openingElement;
      if (import_core4.types.isJSXIdentifier(openingElement.name)) {
        const name = openingElement.name.name;
        const componentType = import_componentList.componentList[importedStyleFunctions[name]];
        if (componentType) {
          switch (componentType) {
            case "Box":
              return handleBox(path);
            case "Flex":
              return handleFlex(path);
            case "Spacer":
              return handleSpacer(path);
            case "Text":
              return handleText(path);
            case "Button":
              return handleButton(path);
            case "Heading":
              return handleHeading(path);
          }
        }
      }
    }
  });
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  processComponents
});
