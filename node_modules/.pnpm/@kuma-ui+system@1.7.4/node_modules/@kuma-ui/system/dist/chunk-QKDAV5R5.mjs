// src/pseudo.ts
var normalizePseudo = (props) => {
  return props.replace("_", ":");
};
var isPseudoProps = (props) => {
  return `${props}`.startsWith("_");
};

export {
  normalizePseudo,
  isPseudoProps
};
