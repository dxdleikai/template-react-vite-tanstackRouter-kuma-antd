import { Plugin } from 'vite';

type PluginOptions = {
    /** Use Rust based WASM compiler instead of Babel */
    wasm?: boolean;
};
declare function kumaUI(options?: PluginOptions): Plugin;

export { kumaUI as default };
