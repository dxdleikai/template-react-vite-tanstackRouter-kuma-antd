"use client";

// src/registry/StyleRegistry.tsx
import React from "react";
import { StyleSheetRegistry } from "./StyleSheetRegistry.mjs";
var StyleSheetContext = React.createContext(
  null
);
StyleSheetContext.displayName = "StyleSheetContext";
function createStyleRegistry() {
  return new StyleSheetRegistry();
}
function StyleRegistry({
  registry: configuredRegistry,
  children
}) {
  const rootRegistry = React.useContext(StyleSheetContext);
  const [registry] = React.useState(
    () => rootRegistry || configuredRegistry || createStyleRegistry()
  );
  return React.createElement(
    StyleSheetContext.Provider,
    { value: registry },
    children
  );
}
function useStyleRegistry() {
  return React.useContext(StyleSheetContext);
}
export {
  StyleRegistry,
  StyleSheetContext,
  createStyleRegistry,
  useStyleRegistry
};
