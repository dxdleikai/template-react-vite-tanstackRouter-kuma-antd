import React from 'react';

declare class StyleSheetRegistry {
    private sheet;
    private serverSideRenderedStyleMap;
    private indexesMap;
    private instancesCountMap;
    constructor();
    add(id: string, css: string): void;
    remove(id: string): void;
    styles(options?: {
        nonce?: string;
    }): (React.DetailedReactHTMLElement<{
        id: string;
        key: string;
        nonce: string | undefined;
        dangerouslySetInnerHTML: {
            __html: string;
        };
    }, HTMLElement> | null)[];
    flush(): void;
    private getServerSideRenderedStyleMap;
}

export { StyleSheetRegistry };
