import { StyleSheet } from './StyleSheet.js';

declare class ServerStyleSheet implements StyleSheet {
    private name;
    private deletedRulePlaceholder;
    private speedy;
    private serverSheet;
    private injected;
    private rulesCount;
    constructor(name: string, speedy?: boolean);
    inject(): void;
    setSpeedy(bool: boolean): void;
    isSpeedy(): boolean;
    insertRule(rule: string, index?: number): number;
    deleteRule(index: number): void;
    flush(): void;
    cssRules(): ReturnType<StyleSheet["cssRules"]>;
    private getSheet;
}

export { ServerStyleSheet };
