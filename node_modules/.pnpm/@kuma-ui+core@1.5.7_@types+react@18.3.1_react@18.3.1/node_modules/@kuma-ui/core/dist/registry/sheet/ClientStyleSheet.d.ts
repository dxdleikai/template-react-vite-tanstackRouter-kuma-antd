import { StyleSheet } from './StyleSheet.js';

declare class ClientStyleSheet implements StyleSheet {
    private name;
    private deletedRulePlaceholder;
    private speedy;
    private tags;
    private injected;
    private rulesCount;
    constructor(name: string, speedy?: boolean);
    inject(): void;
    isSpeedy(): boolean;
    setSpeedy(bool: boolean): void;
    insertRule(rule: string, index?: number): number;
    deleteRule(index: number): void;
    flush(): void;
    cssRules(): ReturnType<StyleSheet["cssRules"]>;
    private makeStyleTag;
    private getSheet;
    private getLatestSheet;
}

export { ClientStyleSheet };
