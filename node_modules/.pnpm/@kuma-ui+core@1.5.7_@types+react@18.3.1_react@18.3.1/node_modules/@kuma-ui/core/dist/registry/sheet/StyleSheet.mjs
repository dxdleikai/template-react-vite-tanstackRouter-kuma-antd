// src/registry/sheet/StyleSheet.ts
import { ClientStyleSheet } from "./ClientStyleSheet.mjs";
import { ServerStyleSheet } from "./ServerStyleSheet.mjs";
import { isBrowser } from "../../utils/isBrowser.mjs";
var StyleSheet = class {
  constructor(name, speedy = false) {
    return isBrowser ? new ClientStyleSheet(name, speedy) : new ServerStyleSheet(name, speedy);
  }
};
export {
  StyleSheet
};
