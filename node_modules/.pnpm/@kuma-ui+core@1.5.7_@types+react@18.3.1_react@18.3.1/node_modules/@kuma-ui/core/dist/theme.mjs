// src/theme.ts
import {
  flattenObject
} from "./utils/object.mjs";
function createTheme(theme) {
  const rawTheme = theme;
  const resolvedTokens = {};
  for (const key in rawTheme) {
    if (key !== "components") {
      resolvedTokens[key] = flattenObject({ [key]: rawTheme[key] });
    }
  }
  return {
    ...resolvedTokens,
    components: rawTheme.components
  };
}
export {
  createTheme
};
