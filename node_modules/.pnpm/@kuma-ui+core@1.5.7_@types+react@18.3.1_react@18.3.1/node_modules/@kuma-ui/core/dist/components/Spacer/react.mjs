// src/components/Spacer/react.tsx
import React from "react";
import { Box } from "../Box/index.mjs";
import { spacerHandler } from "./handler.mjs";
import { theme } from "@kuma-ui/sheet";
import { defaultSpacerTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var Spacer = forwardRef(
  ({ as: Component = defaultSpacerTag, children, size, horizontal, ...props }, ref) => {
    props = {
      ...spacerHandler({ size, horizontal }),
      ...props
    };
    const variant = props.variant ? theme.getVariants("Spacer")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variant,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  Spacer
};
