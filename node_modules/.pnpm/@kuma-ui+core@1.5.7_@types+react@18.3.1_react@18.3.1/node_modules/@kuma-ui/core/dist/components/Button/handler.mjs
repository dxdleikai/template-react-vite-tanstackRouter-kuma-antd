// src/components/Button/handler.ts
var defaultButtonTag = "button";
var buttonSpecificProps = [];
var isButtonProps = (propName) => {
  return buttonSpecificProps.some((k) => k === propName);
};
var buttonDefaultProps = {
  cursor: "pointer"
};
var buttonHandler = (props) => ({});
export {
  buttonDefaultProps,
  buttonHandler,
  defaultButtonTag,
  isButtonProps
};
