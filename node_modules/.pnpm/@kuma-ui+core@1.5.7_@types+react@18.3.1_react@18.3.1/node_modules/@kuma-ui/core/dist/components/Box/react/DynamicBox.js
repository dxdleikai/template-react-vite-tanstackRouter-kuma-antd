"use strict";
"use client";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var DynamicBox_exports = {};
__export(DynamicBox_exports, {
  DynamicBox: () => DynamicBox
});
module.exports = __toCommonJS(DynamicBox_exports);
var import_react = __toESM(require("react"));
var import_isBrowser = require("../../../utils/isBrowser");
var import_StyleRegistry = require("../../../registry/StyleRegistry");
var import_utils = require("./utils");
var import_sheet = require("@kuma-ui/sheet");
var import_forwardRef = require("../../forwardRef");
const defaultRegistry = (0, import_StyleRegistry.createStyleRegistry)();
const useInsertionEffect = import_react.default.useInsertionEffect || import_react.default.useLayoutEffect;
const DynamicBox = (0, import_forwardRef.forwardRef)(
  ({ as: Component = "div", children, variant, IS_KUMA_DEFAULT, ...props }, ref) => {
    const registry = (0, import_StyleRegistry.useStyleRegistry)() || defaultRegistry;
    const variantStyle = (() => {
      if (!variant)
        return {};
      if (!!IS_KUMA_DEFAULT)
        return {};
      return import_sheet.theme.getVariants("Box")?.variants?.[variant];
    })();
    const { dynamicProps, restProps } = (0, import_utils.extractDynamicProps)({
      ...variantStyle,
      ...props
    });
    const { className, css } = (0, import_utils.getCachedStyle)(dynamicProps);
    const box = import_react.default.createElement(
      Component,
      {
        ref,
        ...restProps,
        className: [restProps.className, className].filter(Boolean).join(" ")
      },
      children
    );
    if (!import_isBrowser.isBrowser) {
      registry.add(className, css);
      return box;
    }
    useInsertionEffect(() => {
      registry.add(className, css);
      return () => {
        registry.remove(className);
      };
    }, [className, css]);
    return box;
  }
);
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  DynamicBox
});
