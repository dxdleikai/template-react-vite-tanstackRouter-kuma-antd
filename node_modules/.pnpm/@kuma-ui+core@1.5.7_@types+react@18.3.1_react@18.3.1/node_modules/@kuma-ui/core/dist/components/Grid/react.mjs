// src/components/Grid/react.tsx
import { theme } from "@kuma-ui/sheet";
import React from "react";
import { Box } from "../Box/index.mjs";
import { defaultGridTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var Grid = forwardRef(
  ({ as: Component = defaultGridTag, children, ...props }, ref) => {
    const variant = props.variant ? theme.getVariants("Grid")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variant,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  Grid
};
