// src/components/VStack/react.tsx
import { theme } from "@kuma-ui/sheet";
import React from "react";
import { Box } from "../Box/index.mjs";
import { defaultVStackTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var VStack = forwardRef(
  ({ as: Component = defaultVStackTag, children, ...props }, ref) => {
    const variant = props.variant ? theme.getVariants("VStack")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variant,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  VStack
};
