// src/components/HStack/react.tsx
import { theme } from "@kuma-ui/sheet";
import React from "react";
import { Box } from "../Box/index.mjs";
import { defaultHStackTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var HStack = forwardRef(
  ({ as: Component = defaultHStackTag, children, ...props }, ref) => {
    const variant = props.variant ? theme.getVariants("HStack")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variant,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  HStack
};
