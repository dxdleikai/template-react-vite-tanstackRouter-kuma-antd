// src/components/Input/react.tsx
import React from "react";
import { Box } from "../Box/index.mjs";
import { theme } from "@kuma-ui/sheet";
import { defaultInputTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var Input = forwardRef(
  ({ as: Component = defaultInputTag, children, ...props }, ref) => {
    const variantStyle = props.variant ? theme.getVariants("Input")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variantStyle,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  Input
};
