// src/components/Text/handler.ts
var defaultTextTag = "p";
var textSpecificProps = [];
var isTextProps = (propName) => {
  return textSpecificProps.some((k) => k === propName);
};
var textDefaultProps = {};
var textHandler = (props) => ({});
export {
  defaultTextTag,
  isTextProps,
  textDefaultProps,
  textHandler
};
