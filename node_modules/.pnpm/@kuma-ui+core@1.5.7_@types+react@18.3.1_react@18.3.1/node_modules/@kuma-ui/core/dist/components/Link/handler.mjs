// src/components/Link/handler.ts
var defaultLinkTag = "a";
var linkSpecificProps = [];
var isLinkProps = (propName) => {
  return linkSpecificProps.some((k) => k === propName);
};
var linkDefaultProps = {
  cursor: "pointer"
};
var linkHandler = (props) => ({});
export {
  defaultLinkTag,
  isLinkProps,
  linkDefaultProps,
  linkHandler
};
