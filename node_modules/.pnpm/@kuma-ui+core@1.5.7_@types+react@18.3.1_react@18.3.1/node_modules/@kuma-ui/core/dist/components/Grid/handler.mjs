// src/components/Grid/handler.ts
var defaultGridTag = "div";
var gridSpecificProps = [];
var isGridProps = (propName) => {
  return gridSpecificProps.some((k) => k === propName);
};
var gridDefaultProps = {
  display: "grid"
};
var gridHandler = (props) => ({});
export {
  defaultGridTag,
  gridDefaultProps,
  gridHandler,
  isGridProps
};
