// src/components/Button/react.tsx
import React from "react";
import { Box } from "../Box/index.mjs";
import { theme } from "@kuma-ui/sheet";
import { defaultButtonTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var Button = forwardRef(
  ({ as: Component = defaultButtonTag, children, ...props }, ref) => {
    const variant = props.variant ? theme.getVariants("Button")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variant,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  Button
};
