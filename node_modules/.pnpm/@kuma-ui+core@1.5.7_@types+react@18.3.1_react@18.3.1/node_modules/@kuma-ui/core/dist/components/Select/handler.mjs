// src/components/Select/handler.ts
var defaultSelectTag = "select";
var selectSpecificProps = [];
var isSelectProps = (propName) => {
  return selectSpecificProps.some((k) => k === propName);
};
var selectDefaultProps = {};
var selectHandler = (props) => ({});
export {
  defaultSelectTag,
  isSelectProps,
  selectDefaultProps,
  selectHandler
};
