// src/components/Text/react.tsx
import React from "react";
import { Box } from "../Box/index.mjs";
import { theme } from "@kuma-ui/sheet";
import { defaultTextTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var Text = forwardRef(
  ({ as: Component = defaultTextTag, children, ...props }, ref) => {
    const variant = props.variant ? theme.getVariants("Text")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variant,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  Text
};
