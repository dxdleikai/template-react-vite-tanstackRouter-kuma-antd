// src/components/Link/react.tsx
import React from "react";
import { Box } from "../Box/index.mjs";
import { theme } from "@kuma-ui/sheet";
import { defaultLinkTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var Link = forwardRef(
  ({ as: Component = defaultLinkTag, children, ...props }, ref) => {
    const variant = props.variant ? theme.getVariants("Link")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variant,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  Link
};
