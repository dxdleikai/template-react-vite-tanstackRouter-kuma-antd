// src/components/Box/handler.ts
var defaultBoxTag = "div";
var boxSpecificProps = [];
var isBoxProps = (propName) => {
  return boxSpecificProps.some((k) => k === propName);
};
var boxDefaultProps = {};
var boxHandler = (props) => ({});
export {
  boxDefaultProps,
  boxHandler,
  defaultBoxTag,
  isBoxProps
};
