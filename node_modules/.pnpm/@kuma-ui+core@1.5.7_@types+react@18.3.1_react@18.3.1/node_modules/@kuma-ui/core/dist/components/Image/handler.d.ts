import { StyledProps } from '@kuma-ui/system';

declare const defaultImageTag = "img";
type ImageSpecificProps = {};
declare const isImageProps: (propName: unknown) => propName is ImageSpecificProps;
declare const imageDefaultProps: StyledProps;
declare const imageHandler: (props: ImageSpecificProps) => {};

export { ImageSpecificProps, defaultImageTag, imageDefaultProps, imageHandler, isImageProps };
