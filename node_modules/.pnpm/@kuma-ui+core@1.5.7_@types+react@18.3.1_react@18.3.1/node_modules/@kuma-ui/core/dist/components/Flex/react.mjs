// src/components/Flex/react.tsx
import { theme } from "@kuma-ui/sheet";
import React from "react";
import { Box } from "../Box/index.mjs";
import { defaultFlexTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var Flex = forwardRef(
  ({ as: Component = defaultFlexTag, children, ...props }, ref) => {
    const variant = props.variant ? theme.getVariants("Flex")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variant,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  Flex
};
