// src/components/Select/react.tsx
import React from "react";
import { Box } from "../Box/index.mjs";
import { theme } from "@kuma-ui/sheet";
import { defaultSelectTag } from "./handler.mjs";
import { forwardRef } from "../forwardRef.mjs";
var Select = forwardRef(
  ({ as: Component = defaultSelectTag, children, ...props }, ref) => {
    const variantStyle = props.variant ? theme.getVariants("Select")?.variants?.[props.variant] : {};
    return /* @__PURE__ */ React.createElement(
      Box,
      {
        as: Component,
        ref,
        ...variantStyle,
        ...props,
        children,
        IS_KUMA_DEFAULT: true
      }
    );
  }
);
export {
  Select
};
