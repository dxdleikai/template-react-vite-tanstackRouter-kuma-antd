/// <reference types="react" />
export type customThemeProps = {
    /**
     * Css 选择器名称 默认全局
     *
     * @default :root
     */
    root?: string;
    /** Tokens */
    token?: {
        [name: string]: string | number;
    };
    /**
     * 是否使用默认的token，默认开启，如果有自定token，会进行合并
     *
     * @default true
     */
    useDefaultToken?: boolean;
    /**
     * 变量前缀
     *
     * @default
     */
    prefix?: string;
    children?: React.ReactNode;
};
export declare const CustomTheme: {
    (props: customThemeProps): import("react").ReactNode;
    defaultToken: {
        [name: string]: string | number;
    };
};
export default CustomTheme;
